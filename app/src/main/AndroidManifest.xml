<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adni_gumilang.pengenalanbuah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityAlpukat"
            android:label="@string/alpukat"></activity>
        <activity android:name=".activity_apel"
            android:label="@string/apel"></activity>
        <activity android:name=".activity_jeruk"
            android:label="@string/jeruk"></activity>
        <activity android:name=".activity_anggur"
            android:label="@string/anggur"></activity>
    </application>

</manifest>